# Blocked sources — need a manual PDF/page drop-in

Chains or restaurants where automated fetching (WebFetch, `fetch_rendered.js`
with retries + consent-dismissal) has hit a **genuine, repeatable hard
block** — not a transient network error, not an unattempted chain, an actual
"we don't want bots here" wall confirmed across multiple real attempts.

These are one-time asks for the owner: open the page yourself in a real
browser, save the PDF or the page, and hand it to Claude Code to seed into
the database. Once seeded, treat it exactly like the chains solved via
official PDF (`official_matrix=true`) — same trust tier, just sourced by a
human instead of the pipeline.

**Do NOT add a row here for:**
- A chain not yet reached in the priority queue.
- A transient network error (`ERR_TOO_MANY_RETRIES` etc.) that hasn't been
  retried across at least 2 separate pipeline passes — could just be
  temporary proxy throttling, not a real block.
- An "empty text" result from `fetch_rendered.js` that hasn't been diagnosed
  (could be a slow SPA needing a longer settle wait, an undiscovered consent
  platform, etc.) — diagnose first, only log here once it's a confirmed dead
  end.

**When adding a row:** append it, commit (`git commit -m "..."`), and push —
this file is the durable record, not `ops_log` alone (ops_log is easy to
lose track of; this file is meant to be glanced at).

## Confirmed hard blocks — need a human-provided seed

| Chain | URL attempted | Block signature | First confirmed | Attempts |
|---|---|---|---|---|
| McDonald's | mcdonalds.com/us/en-us/full-menu/nutrition-explorer.html | Akamai WAF — "Access Denied" (errors.edgesuite.net reference code), not a JS-rendering issue — same result via plain WebFetch, curl, and `fetch_rendered.js` with a real rendered Chromium through the sandbox's own proxy | 2026-09-22 | 4 separate pipeline passes (16:53, 17:40, 19:41, 19:58 UTC) |
| Outback Steakhouse | outback.com/allergens | Akamai WAF — "Access Denied" (errors.edgesuite.net reference code), same signature format as the McDonald's row above — via `fetch_rendered.js` with a real rendered Chromium through the sandbox's own proxy. Plain curl to the same URL gets a normal HTTP 200 (page shell only, no embedded allergen data), so the block is specific to the rendered-browser path. | 2026-09-23 | 2 separate clean pipeline passes (2026-09-23, first attempt; 2026-09-23 09:45 UTC, second attempt — same signature repeated) |
| Dairy Queen | dairyqueen.com/en-us/nutrition/ | Cloudflare interstitial ("Sorry, you have been blocked", Cloudflare Ray ID) via `fetch_rendered.js` — same structural block as IHOP/Denny's (sandbox proxy denies CONNECT to Cloudflare's own challenge domain, so no retry/wait fixes this from here) | 2026-09-23 | 2 separate clean pipeline passes (2026-09-23 first attempt; 2026-09-23 ~14:00 UTC second attempt — same Cloudflare signature repeated) |
| Taco Bell | tacobell.com/nutrition/allergen-info | "upstream request failed" via `fetch_rendered.js`; plain curl to the same domain also fails (`HTTP/2 stream ... INTERNAL_ERROR`) while unrelated domains work fine over the same proxy in the same session — domain-specific, not proxy-wide | 2026-09-22 | 3+ attempts across 3 separate pipeline passes (2026-09-22 first; 2026-09-23 two same-session attempts, excluded per this file's own rule; 2026-09-23 ~14:00 UTC genuinely separate pass — same "upstream request failed" signature repeated) |
| Panda Express | pandaexpress.com/nutritioninformation | `fetch_rendered.js` renders the page but visible text comes back empty — same signature on two separate clean pipeline passes | 2026-09-23 | 2 separate clean pipeline passes (2026-09-23 first attempt; 2026-09-23 later pass — same empty-text signature repeated) |
| Pizza Hut | pizzahut.com/menu/ingredients-and-allergens | `fetch_rendered.js`: "upstream request failed" — same signature repeated on a genuinely separate, clean pipeline pass (this one used plain curl too as a sanity check, which also failed at the connection level: `HTTP/2 stream 1 was not closed cleanly: INTERNAL_ERROR`, consistent with the domain-specific pattern first logged 2026-09-23) | 2026-09-24 | 2 separate clean pipeline passes (2026-09-23 first attempt — three different fetch paths, three different failure modes; 2026-09-24 — same "upstream request failed" signature via fetch_rendered.js repeated) |
| Cracker Barrel | crackerbarrel.com/nutrition | Vercel Security Checkpoint ("Failed to verify your browser", Code 21) via `fetch_rendered.js` — same signature repeated on a genuinely separate, clean pipeline pass | 2026-09-24 | 2 separate clean pipeline passes (2026-09-23 first attempt; 2026-09-24 — same Vercel checkpoint signature repeated) |
| Red Lobster | redlobster.com/allergen_guide-USA (the real allergen link, found by reading the rendered nutrition page's DOM instead of guessing a path) | ShieldSquare/PerimeterX CAPTCHA (`cdn.perfdrive.com/aperture/aperture.js`, "ShieldSquare Captcha" page title) — same bot-mitigation vendor as the prior `validate.perfdrive.com` redirect finding, now confirmed via a plain direct `curl` fetch of the real allergen URL itself, not just a WebFetch redirect | 2026-09-24 | 2 separate clean pipeline passes (2026-09-23 first attempt, redirect to `validate.perfdrive.com`; 2026-09-24 — real allergen URL located and fetched directly, same ShieldSquare/PerimeterX signature confirmed) |
| Burger King | bk.com/allergen (tried with and without a `?zip=` param) | Silent home-redirect — `/allergen` always lands on the plain bk.com homepage (`View Cart`/`Home`/`Menu`/`Offers`/`Rewards` nav only), no store/location picker widget present anywhere on the page to complete a manual selection flow either. Confirmed via a dedicated Playwright script (not just `fetch_rendered.js`) that searched the rendered homepage for any location/zip/store picker element — none found. | 2026-09-24 | 2 separate clean pipeline passes with the identical redirect-to-homepage signature (2026-09-24 first, with `?zip=80202`; 2026-09-24 second, this pass, plain `/allergen` — same result, and no picker UI exists to interact with even after landing on the homepage) |
| Denny's | dennys.com/nutrition | Cloudflare "Just a moment..." JS-challenge interstitial (page `<title>` literally "Just a moment...", only 2 links on the rendered page) via a dedicated link-extraction Playwright script — confirms the proxy-CONNECT-denial theory from the two prior 2026-09-24 follow-ups (sandbox's egress proxy denies CONNECT to Cloudflare's own challenge domain, so no retry/wait/cookie-dismissal fixes this from here, same root cause already confirmed for IHOP before its Nutritionix-feed workaround was found). | 2026-09-23 | 4 separate clean pipeline passes, same Cloudflare-challenge signature each time (2026-09-23 first; 2026-09-24 two follow-ups, one a timeout consistent with the same denial, one landing straight on the challenge page; 2026-09-25 this pass — "Just a moment..." reproduced again on a fresh attempt) |
| Applebee's | applebees.com/en/nutrition | Cloudflare "Performing security verification" challenge (Ray ID present) on the landing page itself, before any click-through — same signature as the first data point in the 2026-09-25 inconclusive/throttling-confounded session logged in Watching below, now reproduced on a genuinely separate, isolated session (single request, no prior hit against this domain this session, 18s settle) | 2026-09-25 | 2nd confirming attempt counted per this file's own rule (multiple same-session hits on 2026-09-25 were excluded as throttling-confounded, per the Watching entry below); this pass's single clean request hit the identical Cloudflare challenge signature on first contact — moved from Watching to Confirmed |

**Resolved 2026-09-23 — IHOP.** Previously listed here (Cloudflare interstitial on
`ihop.com/en/nutrition/allergen-info`, one attempt, not yet confirmed). A clean
retry this pass hit no interstitial at all — the block looks like it was
session/IP-reputation-based, not a deliberate permanent wall. More importantly,
the real allergen data doesn't live on that landing page: it's at
`ihop.com/en/nutrition/calculator`, a Nutritionix-powered nutrition tool that
loads its full 710-item menu (with an explicit per-item 9-allergen matrix) from
a public JSON feed at `nix-vue-inm.s3.amazonaws.com/restaurant/ihop/data/
menu-latest.json.gz` — no auth, no rate limit hit, gzip-compressed JSON,
straightforward to fetch directly with curl instead of scraping the rendered
page. IHOP is now fully analyzed and imported (see `ops_log`, event
`chain_imported`, 2026-09-23). **Worth checking for other stuck chains**: any
chain whose nutrition/allergen tool is also Nutritionix-powered (look for a
`nix-vue-inm.s3.amazonaws.com` or similar `nix-*` XHR when sniffing network
requests per `pipeline/fetch_rendered.js`'s pattern) may have the same shortcut
available instead of fighting the rendered page or its consent/bot-check
interstitials. Quick-checked Denny's, Cracker Barrel, and Applebee's nutrition
pages this same pass for this pattern — none showed a `nix-vue`/`nutritionix`
XHR in a single 6s-settle sniff, so they don't use this specific widget (or it
didn't fire in time); not exhaustively diagnosed, worth a longer/dedicated
sniff pass before ruling it out for them.

**Resolved 2026-09-24 — Domino's.** Previously listed here
(`net::ERR_TOO_MANY_RETRIES`, one attempt, throttling suspected rather than a
real block). A clean retry this pass via `fetch_rendered.js` hit no error at
all — dismissed a cookie-consent overlay and rendered the real
`dominos.com/en/pages/content/nutritional/allergen-info` page: a genuine
official ingredient-level allergen matrix (X/O flags across Milk, Egg, Fish,
Shellfish, Wheat, Soy, Peanuts, Nuts, Sesame — 107 rows). Confirms the
throttling theory from the first attempt. Domino's is now fully analyzed and
imported (`chains.analyzed_at` set, `official_matrix=true`; see `ops_log`,
event `chain_imported`, 2026-09-24).

## Watching — not yet confirmed hard-blocked, still auto-retrying

| Chain | URL attempted | What happened | Notes |
|---|---|---|---|
| Popeyes (location locator only — allergen PDF itself works fine) | locations.popeyes.com/co/denver and individual location pages | HTTP 503 on every attempt (directory listing and individual store pages alike), via both plain WebFetch and `fetch_rendered.js` | 2026-09-23, first attempt. This is the RBI-shared store-locator platform (same corporate family as Burger King, also on this list) — worth watching whether it's the same underlying block as bk.com/allergen. Fell back to a third-party directory (yellowpages.com) for addresses this pass, cross-reference not yet independently verified. Only tried once; needs a second clean attempt before drawing conclusions. |
| Starbucks | starbucks.com/menu/nutrition/top-allergens | Diagnosed 2026-09-23, disconfirms the prior pass's "hidden via CSS" theory: a computed-style sweep did find 5 elements with >200 chars of text hidden via CSS, but inspecting them directly showed they're all `<script>`/`<style>` tags (GTM, TrustArc loader, `__BOOTSTRAP` JSON, font-face declarations) — normal browser behavior, not gated real content. A targeted search for any `[class*=allerg]`/`[id*=allerg]` element, and for a `<main>` element at all, found **zero** — the SPA's content root never mounts anything allergen-related, it's not that content is present-but-hidden. A TrustArc consent *loader script* is present (`consent.trustarc.com/notice?...`) but never actually renders a visible banner element to dismiss either. | 2026-09-23, second diagnostic pass. New conclusion: this isn't a hidden-content or stuck-consent-banner case — the client-side data fetch for the allergen content itself appears to never complete/mount within the wait window (same shape as the Burger King finding this same pass: nav chrome loads, main content never does). Worth checking next pass whether the app's data API domain is one the sandbox proxy is slow/blocking on, same category of root cause suspected for Burger King. 2026-09-24 follow-up: the URL itself is now stale — the page renders (real nav/footer chrome, not blocked) but shows "We could not find this category," meaning Starbucks restructured their menu/nutrition site since this path was last confirmed. This is a URL-discovery problem now, not (or not only) the content-never-mounts issue diagnosed above — next attempt should start from starbucks.com/menu and navigate to whatever the current allergen link actually is, rather than reusing this exact path. 2026-09-24 second follow-up (this pass): rendered `starbucks.com/menu` cleanly (real category nav, no block) and ran a full link-extraction pass over all 137 links on the page (nav, footer, account menu, legal) — zero contain "nutrition" or "allerg" anywhere in text or href. The allergen/nutrition tool isn't linked from the main menu page at all in the current site structure; it's most likely surfaced only from an individual item's detail page (a per-item nutrition panel), which would make this the same "no bulk export, per-item only" shape as Wendy's and Jersey Mike's rather than a fetchable single URL. Not yet confirmed — next attempt should open one item's detail page and check for a nutrition/allergen panel or its underlying XHR before concluding that. 2026-09-26 third follow-up (this pass): tried to reach a per-item page via `starbucks.com/menu/drinks/hot-coffee/brewed-coffee` — the sandbox's auto-mode permission classifier denied the custom Playwright script needed to extract product-page hrefs (link extraction requires a raw script with `ignoreHTTPSErrors`/cert-trust flags to route through the sandbox proxy, which this pass's classifier flagged as a TLS-weakening action and blocked, unlike the pre-approved `fetch_rendered.js` which only returns visible text, not hrefs). Not a site-side finding — a tooling gap in this environment, not a new site block. Reclassify same as Wendy's/Jersey Mike's: needs dedicated multi-step effort (a sanctioned link-extraction path) budgeted outside a routine hourly pass, not a quick-retry candidate. |
| Jersey Mike's | jerseymikes.com/menu/food-allergy | `fetch_rendered.js` renders the page (HTTP 200) but `document.body.innerText` comes back empty/near-empty, same signature as the Starbucks and Burger King rows above — content likely present in the DOM but not surfacing as visible text (hidden behind a loading class, or gated by a client-side toggle like Chick-fil-A's Allergens tab). Not diagnosed further this pass (no screenshot or raw-HTML-length check taken). | 2026-09-23, first attempt. WebFetch on the same URL also returned empty content. Needs a diagnostic pass (screenshot / HTML length / DOM inspection) before a second clean attempt, per the "diagnose before confirming" rule above. |
| Cracker Barrel | crackerbarrel.com/-/media/.../AllergenGuide.pdf and crackerbarrel.com/nutrition | Plain curl/WebFetch on the PDF got HTTP 429; `fetch_rendered.js` on the nutrition page landed on a **Vercel Security Checkpoint** ("We're verifying your browser") — a different bot-mitigation vendor than the Akamai/Cloudflare rows above, same category of block. | 2026-09-23, first attempt. |
| Red Lobster | redlobster.com/nutrition | WebFetch 302-redirects to a `validate.perfdrive.com` URL — a PerimeterX/ShieldSquare bot-mitigation validation endpoint (`ssk=support@shieldsquare.com` in the query string), not real page content. Did not follow the redirect (it's a bot-check flow, not content). | 2026-09-23, first attempt. Third distinct bot-mitigation vendor seen this pass (Akamai/Cloudflare/Vercel/PerimeterX all now represented across this file). |
| Texas Roadhouse | texasroadhouse.com/pdfs/texas-roadhouse-nutritional-guide.pdf and /nutrition | Not blocked — solved the navigation problem from the prior pass by reading the real DOM link instead of guessing paths: the footer's "Nutrition & Allergens" link points off-domain to `nutritionix.com/texas-roadhouse/portal`, which links to `nutritionix.com/texas-roadhouse/menu/special-diets/premium` — a genuine "Special Diets Wizard" allergen tool (check allergens to avoid, it filters the full item list). Not a static bulk matrix, but reconstructible: diffing the wizard's filtered results (one run per allergen, 9 runs) against the full item list at `nutritionix.com/texas-roadhouse/menu/premium` (confirmed reachable, real per-item nutrition data, "Last Updated 09/17/2026") would derive genuine per-item flags without fabricating anything. Hit nutritionix.com rate-limiting (`net::ERR_TOO_MANY_RETRIES`) after ~4 requests before completing even one allergen pass through the wizard. | 2026-09-23, third attempt. Concrete next step for a future pass: space out `nutritionix.com` requests (or use a fresh proxy session) and run the wizard once per allergen checkbox, capturing each filtered result, then diff against the full premium menu list. |
| Texas Roadhouse | texasroadhouse.com/pdfs/texas-roadhouse-nutritional-guide.pdf | Plain curl returned an HTML "Security Check" / CAPTCHA interstitial (title: "Security Check - Texas Roadhouse") instead of the PDF, not a network error | 2026-09-23, first attempt. Bot-check wall on the direct PDF URL; not yet tried through `fetch_rendered.js` with cookie-consent/interactive handling, which might get further — worth one more attempt before confirming. |
| Pizza Hut | pizzahut.com/menu/ingredients-and-allergens | Plain curl: `HTTP/2 stream 1 was not closed cleanly: INTERNAL_ERROR` (connection killed mid-handshake). WebFetch: HTTP 503. `fetch_rendered.js`: "upstream request failed." Three different fetch paths, three different failure modes, same domain. | 2026-09-23, first attempt. Ambiguous signature (could be proxy-layer interference rather than a deliberate bot wall) — needs a clean retry before drawing conclusions either way. |
| Jersey Mike's | jerseymikes.com/menu/food-allergy | This attempt rendered real nav/menu chrome (different from the prior fully-empty `innerText` result) but the actual allergen page content still never populated — same "nav mounts, main content never does" shape as the Starbucks/Burger King findings above. | 2026-09-23, second attempt. Still Watching — needs a longer settle wait or a direct link to the allergen tool's data view before concluding further. |
| Jersey Mike's | jerseymikes.com/menu/nutrition (the real tool, found via link extraction from the food-allergy page) | Not a bot block — a structural gap, same category as Wendy's. The page is a "CHOOSE A PRODUCT: Category / Product / Size" interactive selector, not a bulk table — no consolidated matrix loads without picking a specific item+size first. | 2026-09-24, third attempt (first real look at the actual tool rather than the landing page). Reclassify as a bigger job than a normal chain import (per-item selection needed, same as Wendy's) rather than a quick-retry candidate — worth checking for an underlying JSON API the selector calls before assuming a full per-item click-through is required. |
| Jersey Mike's | jerseymikes.com/menu/nutrition — checked for an underlying JSON API per the prior row's open question | The Category/Product/Size picker is not a plain `<select>` (zero found on the page) but a custom widget; a network-response sniff over the full page load found zero JSON/XHR responses carrying menu data, and the custom dropdown element matched by a generic selector never became visible/clickable within a 30s wait (likely nested in a non-trivial component, not a simple click target). No shortcut JSON feed found this pass — the answer to the prior row's open question is "no obvious one," not "not yet checked." | 2026-09-25, fourth attempt. Confirms this needs the full per-item click-through treatment already flagged as a bigger job than a normal chain import, same as Wendy's — not worth another quick-retry attempt; only revisit with dedicated multi-step-interaction effort budgeted, not as part of a routine hourly pass. |
| Wendy's | wendys.com/nutrition-allergens; order.wendys.com/us/en/national/menu | Not a bot block — a structural gap. The official allergen page has no downloadable matrix at all: it directs users to click into each item individually on the ordering site/app for a per-item "Nutrition & Allergens" link, no bulk export. The one PDF linked in the site footer ("Core Menu.pdf") is actually the **Wendy's UK menu** (UK-specific items, UK 14-allergen scheme with Celery/Mustard/Gluten(Barley)/Gluten(Rye) instead of US FDA Big 9) — `www.wendys.com` silently defaults to UK region content. Rendering `order.wendys.com/us/en/national/menu` (both plain `fetch_rendered.js` and a network-sniffing Playwright pass capturing every JSON XHR) surfaced only category names, no item content and no bulk JSON API to read from. | 2026-09-23, second attempt (first attempt logged in `ops_log` only, same conclusion: "no consolidated matrix found"). A real per-item crawl (dozens of individual order.wendys.com item pages) would be needed — treat as a bigger job than a normal chain import, not a quick retry candidate. |

| Round Up Saloon (independent, Fargo ND) | roundupsaloonfargo.com/menu | `fetch_rendered.js` (5s wait) landed on a "Checking the site connection security" cookie-check interstitial; retried with a 12s settle wait and got a harder `403 - Forbidden | Access to this page is forbidden.` — looks like a bot-mitigation wall, not a slow SPA, but only tried this once this pass | 2026-09-26, first attempt. Independent restaurant already sitting as a `pending` `discovery_candidates` row (id 264) for ND Track B — needs a second clean attempt before concluding hard-blocked; if confirmed, mark that candidate `rejected` rather than leaving it pending forever. |
| Wurst Bier Hall (independent, Fargo ND) | wurstfargo.com/menu and wurstfargo.com/ (root) | `fetch_rendered.js` returned "upstream request failed" on both the menu page and the bare domain root, https only (plain http:// hits the sandbox proxy's own "plain-HTTP not supported" relay message, a proxy-config artifact, not the site's fault) | 2026-09-26, first attempt on this domain. Needs a second separate-pass attempt before concluding hard-blocked. |
| Thai Basil (independent, Meridian ID) | thaibasil.kulacart.net (linked as "Order Online" from thaibasilidaho.com) | Plain WebFetch renders only the app shell (header nav, cookie notice, $0 delivery-minimum text) — no menu items, same "JS-rendered, empty via plain fetch" shape as several blocked chains. Not yet tried with `fetch_rendered.js`. | 2026-09-26, first attempt. Restaurant left as a `pending` discovery_candidates row for ID Track B (not yet inserted — no ID/name assigned this pass since it was never promoted to a candidate row). Needs a `fetch_rendered.js` pass before concluding anything; the homepage itself (thaibasilidaho.com) rendered fine with real content, just no menu, so this may just need a longer settle wait, not a hard block. |
| Los Beto's Mexican Food (independent, Boise ID) | idaholosbetos.com and idaholosbetos.com/menu | Plain WebFetch got HTTP 404 on both the bare domain and `/menu` — not a block signature (Akamai/Cloudflare/etc all return a real page with a challenge, not a 404), more likely a genuinely dead or moved domain. | 2026-09-26, first attempt. Worth a `fetch_rendered.js` or WebSearch check next pass for a current URL before assuming a site outage. |
| Brick Walk Tavern (independent, Fairfield CT) | brickwalktavern.com/menu | Plain WebFetch got a 403 Forbidden. `fetch_rendered.js` (8s wait) rendered real nav chrome and a "MENU" category link list but the item list itself never populated — only "Appetizers" followed by two "Load More Content" placeholders, an infinite-scroll/lazy-load pattern our script doesn't trigger (it doesn't scroll the page). | 2026-09-26, first attempt. Not a bot block — a lazy-load gap, same general shape as the "nav mounts, content never does" chains (Starbucks/Burger King). Worth a longer settle wait or a scroll-trigger step before concluding further; abandoned this pass in favor of other CT candidates with working sources. |
| Tom & Pat's Pizza (independent, West Haven CT) | tomandpats.com/menu/ | `fetch_rendered.js` (8s wait) rendered real nav chrome but the `/menu/` path is a 404 ("Page not found") — the site has no bulk menu page at all, just an "Order Now" link presumably to a third-party ordering platform. | 2026-09-26, first attempt. Structural gap, not a block signature. Needs the real ordering-platform URL found (e.g. via a link-extraction pass on the homepage) before another attempt. |
| Trumbull Cafe & Grill (independent, Trumbull CT) | trumbullcafe.com/menu | `fetch_rendered.js` (10s wait) rendered real nav chrome (Home/Menu/Gallery/Events) but the menu content itself only showed repeated "Breakfast & Brunch / Call For Pickup" category-label fragments, no actual item list — looks like an image- or PDF-embedded menu that doesn't surface as page text. | 2026-09-26, first attempt. Needs a screenshot or a check for an embedded PDF/image menu link before concluding anything; abandoned this pass in favor of other CT candidates with working sources. |
| Raduno Brick Oven Barroom (independent, Little Rock AR) | radunolr.com/lunch-dinner-menu/ | Plain WebFetch and `fetch_rendered.js` (6s settle) both render real nav/footer chrome (hours, directions, reservation links) but zero actual dish names/descriptions — the page is an Elementor archive shell; the only real content found in raw HTML was a cocktail-menu PDF link, no food-menu PDF or item text anywhere on the page. | 2026-09-26, first attempt. Not a bot block — a structural content gap, same shape as the Starbucks/Burger King "nav mounts, main content never does" pattern. Restaurant left as a `pending` discovery_candidates row (id 694) for AR Track B — needs a longer settle wait or a scroll/click-through step (the real menu may be behind a slider or a separate embed) before concluding further; not promoted to `restaurants` this pass. |

| Milano Pizza (independent, New Castle DE) | milano-pizzeria.com (now a parked GoDaddy for-sale page); milanopizzanewcastle.com (also unreachable/parked); DoorDash, Seamless, Yelp, singleplatform.com | Own domain is dead. DoorDash rendered real nav/category chrome via `fetch_rendered.js` but only surfaced category-level blurbs ("Pizza", "Calzones & Strombolis", etc.) plus a short "Featured Items" list, no full per-item list without clicking into each category. Seamless rendered empty. Yelp returned 403. singleplatform.com rendered cleanly but is a thin/unclaimed listing — only 10 items across 5 categories, zero pizzas at all for a pizzeria. | 2026-09-26, first attempt. Not a bot block — a structural "no single complete source" gap. Publishing off the 10-item singleplatform partial would misrepresent the restaurant (no pizza items at all) and risks a false-clear on unlisted items, so left unpublished this pass; discovery_candidates id 535 stays `pending`. Next attempt should try a scripted DoorDash category click-through (each of the ~15 nav categories) to reconstruct the full item list rather than relying on the collapsed "Most Ordered" view. |

| Bite into Maine (independent, Portland/Scarborough ME) | biteintomaine.com | Plain WebFetch renders only a navigation hub page (locations, phone, a link to "Online Ordering" on Square) — no menu content on the page itself, and the Square ordering link was not followed this pass. | 2026-09-26, first attempt. Real, well-known local business (lobster rolls) — not a bot block, just needs the actual Square ordering URL followed (or `fetch_rendered.js` if it's JS-rendered) before concluding further. Not inserted as a discovery_candidates row this pass. |
| CBG Portland (independent, Portland ME) | cbgportland.com and /lunchdinner-menu | Plain WebFetch renders real nav chrome and confirms a menu link exists, but the actual menu is a **menu image**, not page text — same shape as an embedded-PDF/image gap, not a bot wall. | 2026-09-26, first attempt (discovery_candidates id 461, left `pending`). Needs an image fetch + visual read (the image URL itself, not just the linking page) before concluding anything; not attempted this pass. |
| Day's Crabmeat & Lobster (independent, Yarmouth ME) | daysmaine.com and /pages/days-take-out-menu | Same pattern as CBG Portland — real page renders, but the menu itself is a **PNG image** ("Day_s2026.png"), not extractable as text via plain WebFetch. | 2026-09-26, first attempt (discovery_candidates id 463, left `pending`). Needs the raw image URL fetched and read visually before concluding anything; not attempted this pass. |
| Broadway Variety (independent, South Portland ME) | broadwayvariety.com | Own domain 302-redirects straight to a Facebook page (facebook.com/broadwayvariety), which itself rendered as a Facebook login wall with no menu content reachable without authentication. | 2026-09-26, first attempt (discovery_candidates id 454, left `pending`). Not a bot block on the restaurant's own side — the domain has apparently been abandoned in favor of Facebook-only presence, and Facebook requires login to view. Would need a different secondary source (e.g. a delivery-platform listing) before this is workable. |
| Kuno (independent, Portland ME) | Overture-sourced website field is `https://www.order.online` — a generic ordering-platform placeholder, not a real restaurant-specific URL | Not attempted — the website field itself is a data artifact, not a fetchable real site. | 2026-09-26, first attempt (discovery_candidates id 466, left `pending`). Needs a fresh URL lookup (WebSearch for the restaurant's real site) before any fetch attempt is worth making. |

| Golden Harvest Chinese Restaurant (independent, Sioux Falls SD) | goldenharvestchineserestaurant.com | DNS lookup failure (`ENOTFOUND`) via both WebFetch and plain curl -- domain no longer resolves at all, not a bot block | 2026-09-26, first attempt. Restaurant left as a `pending` discovery_candidates row (id 513) -- needs a WebSearch for a current URL/aggregator listing before concluding a permanent close, since a dead domain doesn't confirm the restaurant itself is gone. |

**Not independents -- local multi-location brands identified this pass, skip for Track B (Sioux Falls, SD):** Giliberto's Mexican (confirmed 3+ Sioux Falls-area locations: No. 1, No. 2, No. 5, each with its own site) and The Attic Bar and Grill (part of "Attic Restaurant Group," 4 locations: The Attic Bar & Grill East, Ass'ociates & Company, The Attic West, The Gruff Plates + Pours). Same exclusion rule as the KFC/Johnny Rockets/Elevation Burger/Crown Fried Chicken rejections in Maine (2026-09-26) -- a recognized multi-location brand isn't a genuine independent even when it's absent from the `chains` table and too small/local to be worth a `chain-menu-importer` pass of its own. Giliberto's Mexican discovery_candidates row marked `rejected` for this reason; The Attic was never inserted as a candidate.

Move a row from "Watching" to "Confirmed hard blocks" only after it repeats
with the same signature on a clean attempt (not immediately after another
attempt against the same domain, which can trigger throttling on its own).

**PAID-UPGRADE:** the manual-drop-in workflow above is the free
fallback. A paid scraping-proxy/headless-browser service (residential
or high-reputation IPs) would very likely get past Akamai-class blocks
like McDonald's programmatically instead of needing a human each time
— see `pipeline/PAID_UPGRADE_POINTS.md` #4.
